{
  "Servers": {
    "1": {
      "Name": "Clank",
      "Group": "Servers",
      "Host": "${CLANK_DATABASE_HOST}",
      "Port": "${CLANK_DATABASE_PORT}",
      "Username": "${POSTGRES_USER}",
      "Password": "${POSTGRES_PASSWORD}",
      "SSLMode": "${CLANK_DATABASE_SSLMODE}",
      "MaintenanceDB": "postgres"
    }
  }
}
